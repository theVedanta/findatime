import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#74B78F",
        },
        secondary: {
            main: "#313131",
        },
    },
});

export default theme;
