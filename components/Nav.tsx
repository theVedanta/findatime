import { Box } from "@mui/material";

const Nav = () => {
    return (
        <Box
            bgcolor="secondary.main"
            display="flex"
            justifyContent="space-between"
            px={10}
            width="100%"
        ></Box>
    );
};

export default Nav;
